# This is a Databricks asset bundle definition for capstone_pipeline.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: capstone_pipeline
  uuid: a95e0071-d102-48f3-a711-6e7b816e6020

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-3583dc88-1cc3.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-3583dc88-1cc3.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/mmanojipec@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/mmanojipec@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: mmanojipec@gmail.com
        level: CAN_MANAGE
